---
- name: Test Connection to Port 443
  hosts: all
  tasks:
    - name: Check if port 5985 is open on Windows server
      ansible.builtin.shell: "nc -zv {{ win_host }} 443"
      register: result
      ignore_errors: yes

    - name: Show result
      ansible.builtin.debug:
        var: result.stdout

